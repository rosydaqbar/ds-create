# ↳ Imagery


> **Initiator gate:** Execute this specification only when this Foundation is in scope. If it already exists, apply the confirmed action (`Keep`, `Audit`, `Improve`, `Refactor`, `Rebuild`, or `Replace`) before changing anything. Actual token names must follow the naming system selected in the initiator; token names shown here are logical roles.

## Required aspect-ratio frames

```text
1:1
4:3
3:2
16:9
2:3
```

## Fit rules

- photography defaults to Fill crop;
- illustrations may use Fit when whitespace is part of the asset;
- faces and critical content must not be cropped by default focal positioning;
- image masks use radius tokens.

## Documentation

Show:
- cropping examples;
- good/bad focal points;
- overlays;
- text over image;
- dark/light scrim examples;
- empty/fallback state.